# 골프총무 — 서비스 기획안 (v2, 재탄생)

## 1. 한 줄 정의
해외 골프 여행 총무가 **경비를 쉽게 입력 → 실시간 환율로 1인당 자동 계산 → N빵 정산을 일행에게 바로 전달**하는, 회원가입형 무료 웹서비스. (모바일 우선)

## 2. 타겟 & 가치
- **타겟**: 4인 내외 해외 골프 여행을 기획·결제·정산하는 30~50대 총무.
- **계산기를 이기는 이유**: ① 환율 자동 환산 ② 1인/팀 비용 N빵 자동 분배 ③ 카톡 정산 문구 자동 ④ 내 여행 기록이 계정에 남음(어디서나 조회).

## 3. 수익 모델
- **무료 + Google AdSense 광고** (목록/계산 화면 하단 배너).
- **향후 유료(프리미엄)**: 견적서 사진/PDF **AI 자동 입력**, 다인원/다중통화 고급 정산, 내보내기(엑셀), 광고 제거 등.
- 광고는 **실제 배포(HTTPS 도메인) + 애드센스 승인** 후 적용. 지금은 자리(placeholder)만 마련.

## 4. 핵심 기능 (MVP 범위)
1. **회원가입 / 로그인** (이메일 + 비밀번호). 로그인 유지(JWT 60일).
2. **내 여행 목록**: 생성 / 조회 / 수정 / 삭제. 각 카드에 제목·국가·1인당 예상액.
3. **여행 편집 = 계산기**:
   - 국가 선택 → 해당국 통화 + 원화(₩)를 기본 통화로 자동 세팅.
   - **실시간 환율 자동 입력**(open.er-api.com) + **수동 입력/수정** 토글.
   - 비용 항목 행: `항목명 · 금액 · 통화 · 1인/팀(N빵)`. **Enter로 다음 행 빠르게 추가**.
   - 결과: **1인당 / 일행 합계** 실시간.
   - **카톡 정산 전달**: "1인 약 ○○원, 항목 내역, 환율 기준" 문구 복사.
4. **모바일 최적화**: 단일 컬럼, 큰 입력칸/버튼, 하단 고정 액션.

## 5. 데이터 모델
- **user**: `{ id, email, passwordHash, createdAt }` (비번은 bcrypt 해시만 저장)
- **trip**: `{ id, userId, title, country, currency(주통화), partySize, fxMode("auto"|"manual"), manualFx{cur:rate}, rows[], createdAt, updatedAt }`
- **row(비용항목)**: `{ name, amount(number|null), currency, scope("person"|"team") }`
- 1인당 = Σ(1인 항목 원화) + Σ(팀 항목 원화)/인원. 합계 = 1인당 × 인원.

## 6. 기술 구조
```
[브라우저(모바일/PC) · 바닐라 JS + Tailwind]
   ⇅ REST(JWT)
[Node·Express 백엔드]  — 인증 + 여행 CRUD + (향후) AI 파서
   ⇅
[JSON 파일 DB]  backend/data/{users,trips}.json  (배포 시 SQLite/Postgres로 교체)
   ↘ 실시간 환율: open.er-api.com (클라이언트에서 직접, 키 불필요)
```
- **인증**: 이메일+비밀번호 → bcrypt 해시 → JWT(Authorization: Bearer). 비밀번호 평문 저장 안 함.
- **API**: `/api/auth/register|login|me`, `/api/trips` (GET/POST), `/api/trips/:id` (PUT/DELETE). 전부 사용자 범위.
- **프론트**: 빌드 없음(순수 `<script>`). 백엔드가 정적 서빙 → 한 주소.

## 7. 화면 구성 (모바일 우선)
1. **로그인/회원가입** — 이메일·비밀번호, 탭 전환.
2. **내 여행 목록** — 카드 리스트 + 「＋ 새 여행」, 카드별 수정/삭제.
3. **여행 편집(계산기)** — 제목·국가·인원 / 항목 빠른입력 / 환율 / 1인당·합계 / 저장 · 카톡전달.
- 하단: AdSense 배너 자리.

## 8. 단계 계획
- **v2.0 (이번 구현)**: 회원가입·로그인, 여행 CRUD, 여행별 계산기(국가·통화·실시간환율·N빵·카톡), 모바일 반응형, 광고 자리.
- **v2.1**: 비밀번호 재설정/이메일 인증, 구글 로그인.
- **v3 (유료)**: 견적서 AI 자동입력(기존 백엔드 /api/parse 재사용), 엑셀 내보내기, 개인별 지출 입력 정산.
- **배포**: 백엔드를 Render/Railway 등에 올리고 도메인 연결 → 총무는 URL 접속만. 그 후 AdSense 신청.

## 9. 비고 (보안/운영)
- JWT 시크릿은 `.env`의 `JWT_SECRET`(배포 시 강력한 값 필수). 사용자 데이터(`backend/data/`)는 git 제외.
- MVP는 JSON 파일 저장(소규모 적합). 트래픽 늘면 DB 전환.
- 견적서 AI(유료 예정)는 비용이 들어 로그인 사용자만, 향후 사용량 제한.
